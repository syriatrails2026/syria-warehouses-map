# مشروع: خريطة مخازن سوريا التفاعلية

هذا الملف مرجع لأي جلسة عمل (Claude Code أو مطوّر) على هذا المشروع. اقرأه قبل البدء بأي تعديل.

الوثيقة التصميمية الكاملة (السياق، المقارنات، القرارات المرفوضة والمقبولة): [docs/superpowers/specs/2026-09-07-syria-warehouses-map-design.md](docs/superpowers/specs/2026-09-07-syria-warehouses-map-design.md)

## الفكرة بجملة واحدة

موقع عرض تفاعلي (marketing/demo site) لصالح وزارة التجارة الداخلية السورية، يعرض توزّع 1,400 مخزن على خريطة سوريا بشكل هرمي (محافظة ← منطقة ← ناحية)، بهدف إقناع مستثمر بالتوقيع والاستثمار في المشروع. **هذه المرحلة = الموقع العام فقط، ببيانات وهمية.** لا يوجد باك-إند ولا لوحة تحكم بعد.

## النطاق (اقرأ هذا جيداً قبل إضافة أي ميزة)

**داخل النطاق:** الهيدر، شريط الإحصائيات، القائمة الجانبية، الخريطة الهرمية التفاعلية، بطاقة تفاصيل المخزن، بيانات وهمية واقعية التوزيع.

**خارج النطاق عمداً — لا تبنِها بدون طلب صريح:**
- لوحة تحكم إدارية / رفع Excel.
- أي خادم، قاعدة بيانات، مصادقة (auth)، أو صلاحيات.
- دعم لغات غير العربية (i18n).
- تجميع نقاط (marker clustering) — الكثافة الحالية (~5 مخازن/ناحية بالمتوسط) لا تبرره.

إذا بدا أن مهمة تتطلب تجاوز هذا النطاق، توقف واسأل بدل الافتراض.

## التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| الإطار | React + Vite | SPA خفيف وسريع، بدون حاجة لـSSR/SEO في هذه المرحلة |
| الخريطة | D3.js (`d3-geo` + `d3-zoom`) | الخريطة مخصّصة (Infographic) وليست خريطة واقعية بـtiles، فمكتبات مثل Leaflet/MapLibre غير مناسبة هنا |
| البيانات الجغرافية | ملفات GeoJSON (3 مستويات) يزوّدها المستخدم | — |
| توليد بيانات وهمية | Node script + `@turf/*` | لضمان وقوع كل نقطة داخل حدود الناحية الفعلية، لا عشوائياً فوق كامل الخريطة |
| الاختبارات | Vitest | لمنطق توليد البيانات وحالة التنقل فقط (لا اختبارات UI شاملة) |
| الاستضافة (مقترحة) | Vercel أو Netlify | موقع ثابت بالكامل |

**لا تستبدل D3.js بمكتبة خرائط تقليدية (Leaflet/MapLibre/Google Maps JS API) بدون نقاش صريح** — هذا قرار مُقارَن ومُعتمد، راجع وثيقة التصميم.

## بنية المجلدات المقترحة

```
src/
  components/
    Header/
    StatsToolbar/
    Sidebar/
    Breadcrumb/
    MapCanvas/
    WarehouseCard/
  data/
    geo/                          # ملفات GeoJSON (محافظات، مناطق، نواحي) — تُزوَّد من المستخدم
    warehouseSource.ts            # الواجهة المجرّدة لمصدر البيانات — نقطة الاستبدال المستقبلية بـAPI حقيقي
    warehouses.generated.json     # ناتج سكربت التوليد، لا يُعدَّل يدوياً
  state/                          # حالة التنقل الهرمي + مزامنة مع query params بالرابط
  styles/                         # ثوابت الهوية البصرية (ألوان، خطوط)
scripts/
  generate-fake-warehouses.mjs    # يولّد warehouses.generated.json من ملفات GeoJSON
docs/superpowers/specs/           # وثائق التصميم المعتمدة
```

## نموذج البيانات (Warehouse)

```ts
interface Warehouse {
  id: string;
  name: string;
  governorateId: string;
  districtId: string;
  subdistrictId: string;
  lat: number;
  lng: number;
  areaM2: number;
}
```

هذا الشكل يجب أن يطابق ما سيُنتَج مستقبلاً من رفع ملف Excel عبر لوحة التحكم (خارج النطاق حالياً) — أي تغيير على هذا الشكل يجب أن يبقى متوافقاً مع تلك الخطة المستقبلية.

**قاعدة أساسية:** كل الواجهة (المكوّنات) تتعامل مع البيانات فقط عبر `warehouseSource.ts` (مثل `getWarehouses()`, `getGovernorateStats()`). **ممنوع** أي مكوّن يستورد `warehouses.generated.json` مباشرة — هذا يكسر إمكانية الاستبدال المستقبلي بـAPI حقيقي دون تعديل الواجهة.

## التنقل الهرمي

4 حالات عرض متسلسلة على نفس `MapCanvas`:
1. **وطنية** — 14 محافظة، تلوين حسب كثافة المخازن (choropleth)، رقم المخازن مكتوب على كل محافظة.
2. **محافظة** — مناطقها، كل منطقة برقم مخازنها.
3. **منطقة** — نواحيها، كل ناحية برقم مخازنها.
4. **ناحية** — نقاط (Pins) فردية بإحداثيات المخازن الحقيقية؛ الضغط على نقطة يفتح `WarehouseCard`.

حالة الاختيار الحالية (محافظة/منطقة/ناحية) تُعكس بـquery params بالرابط (`?gov=..&region=..&sub=..`) — زر رجوع المتصفح يجب أن يرجع مستوى للخلف بشكل طبيعي. القائمة الجانبية، الـBreadcrumb، والخريطة يجب أن تبقى متزامنة دائماً مع نفس حالة الاختيار المركزية — لا تُبقِ حالة منفصلة بكل مكوّن.

وحدة إدارية بدون مخازن **تظهر دائماً** بلون محايد و"0 مخازن" — لا تُخفى أبداً من الخريطة أو القائمة.

## الهوية البصرية

معتمدة عبر مقارنة بصرية مباشرة مع صاحب المشروع (وليست افتراضاً) — لا تُغيَّر بدون نقاش.

**الألوان** (مستوحاة من العلم السوري الجديد؛ الأحمر مُستبعَد عمداً من اللوحة الأساسية):

```css
--color-green-dark:   #0f2a1c;  /* أساسي: هيدر، نصوص مهمة */
--color-green-mid:    #1d4a30;  /* تمييز، حالة نشطة */
--color-green-light:  #2f6b46;  /* تفاعلات، hover */
--color-gold:         #c8a84b;  /* تمييز، أرقام بارزة */
--color-sand:         #dcc27a;  /* خلفيات ثانوية */
--color-bg-warm:      #f7f5ef;  /* خلفية أساسية */
--color-bg-warm-alt:  #faf9f6;
--color-text:         #1f2d24;  /* نص أساسي */
```

**الخط:** Tajawal أو IBM Plex Sans Arabic (Google Fonts) — يُختار أحدهما عند بدء التنفيذ الفعلي حسب توفر الأوزان المطلوبة.

**الاتجاه:** `dir="rtl"` على مستوى الصفحة بالكامل. الموقع عربي فقط، لا حاجة لبنية i18n.

**النمط العام:** شبه رسمي، مؤسسي، نظيف، بدون زخرفة زائدة. لا خرائط واقعية (tiles)، لا تفاصيل شوارع، لا عناصر من خارج سوريا على الخريطة.

## اتفاقيات عامة

- المشروع كله بالعربية — لا نصوص إنكليزية بواجهة المستخدم إلا أسماء تقنية بالكود.
- اسم المشروع النهائي غير محسوم — استخدم اسماً عاماً/وصفياً مؤقتاً من ثابت واحد قابل للتغيير، لا تُدرجه بأماكن متفرقة بالكود.
- لا تضف مصادقة، قاعدة بيانات، أو أي بنية باك-إند بدون طلب صريح — النطاق الحالي static site بالكامل.
- التزم بـYAGNI: لا تبنِ تجريداً أو ميزة "لأنها قد تُفيد لاحقاً" خارج ما هو موثّق صراحة كنقطة استبدال مستقبلية (`warehouseSource.ts`).
